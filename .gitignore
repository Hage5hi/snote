# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (use .env.example for reference)
.env
.env.local
.env.*.local

# Lockfiles from other package managers — project uses bun.lock.
# Prevents accidental drift when someone runs `npm install` or an old bun.
package-lock.json
yarn.lock
pnpm-lock.yaml
bun.lockb

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
